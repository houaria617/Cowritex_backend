[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings
markers =
    unit: Pure unit tests — no HTTP, no DB
    integration: Integration tests with TestClient + mocked DB
    e2e: End-to-end tests requiring real Supabase (skipped by default)